<Window x:Class="FileManagerSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FileManagerSystem.ViewModels"
        mc:Ignorable="d"
        Title="文件管理器 - 操作系统项目" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 菜单栏 -->
        <Menu Grid.Row="0" Background="#F0F0F0">
            <MenuItem Header="文件">
                <MenuItem Header="新建文本文件" Click="NewTextFile_Click"/>
                <MenuItem Header="新建文件夹" Click="NewFolder_Click"/>
                <Separator/>
                <MenuItem Header="退出" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="编辑">
                <MenuItem Header="删除" Click="Delete_Click"/>
                <MenuItem Header="重命名" Click="Rename_Click"/>
            </MenuItem>
            <MenuItem Header="查看">
                <MenuItem Header="刷新" Click="Refresh_Click"/>
            </MenuItem>
        </Menu>

        <!-- 工具栏 -->
        <ToolBar Grid.Row="1" Background="#F8F8F8">
            <Button Click="GoBack_Click" ToolTip="后退">
                <TextBlock Text="← 后退"/>
            </Button>
            <Button Click="GoForward_Click" ToolTip="前进">
                <TextBlock Text="→ 前进"/>
            </Button>
            <Button Click="Up_Click" ToolTip="向上">
                <TextBlock Text="↑ 向上"/>
            </Button>
            <Separator/>
            <Button Click="Refresh_Click" ToolTip="刷新">
                <TextBlock Text="刷新"/>
            </Button>
            <Separator/>
            <TextBlock Text="路径:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox x:Name="PathTextBox" Width="300" VerticalAlignment="Center" IsReadOnly="True"/>
        </ToolBar>

        <!-- 文件列表 -->
        <ListView Grid.Row="2" x:Name="FileListView" 
                  MouseDoubleClick="FileListView_MouseDoubleClick"
                  Background="White">
            
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="300" DisplayMemberBinding="{Binding DisplayName}"/>
                    <GridViewColumn Header="修改时间" Width="150" DisplayMemberBinding="{Binding ModifiedTime}"/>
                    <GridViewColumn Header="大小" Width="100" DisplayMemberBinding="{Binding SizeText}"/>
                </GridView>
            </ListView.View>

            <ListView.ContextMenu>
                <ContextMenu x:Name="FileContextMenu">
                    <MenuItem Header="打开" Click="Open_Click" FontWeight="Bold"/>
                    <Separator/>
                    <MenuItem Header="新建">
                        <MenuItem Header="文本文件" Click="NewTextFile_Click"/>
                        <MenuItem Header="文件夹" Click="NewFolder_Click"/>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="删除" Click="Delete_Click"/>
                    <MenuItem Header="重命名" Click="Rename_Click"/>
                    <Separator/>
                    <MenuItem Header="属性" Click="Properties_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="3" Background="#F0F0F0">
            <StatusBarItem>
                <TextBlock x:Name="ItemCountTextBlock" Text="项目: 0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="DiskUsageTextBlock" Text="磁盘使用: 0/1024 块 (0.0%)"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window> 