<Window x:Class="FileManagerSystem.Views.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message}" Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="1" x:Name="InputTextBox" Text="{Binding Result, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" Padding="5"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="75" Height="25" Margin="0,0,5,0" IsDefault="True" Click="OK_Click"/>
            <Button Content="取消" Width="75" Height="25" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window> 